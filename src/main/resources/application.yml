spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ENC(+3wh4JOYnVjQ/98w29pVsyCVYs99YMtP)
    username: ENC(2kd5AG9x7ZNYS7WxB8G10Ro4CFTdPzgc)
    password: ENC(VT2qsmopni291+Mdf0Kp/3/8FVihK52R)

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      #        show_sql: true // sys out으로 로그가 찍힘
        format_sql: true
        default_batch_fetch_size: 100

jasypt:
  encryptor:
    bean: jasyptStringEncryptor

logging:
  level:
    org.hibernate.SQL: debug # 로거로 로그가 찍힘
#    org.hibernate.type: trace

server:
  port: 8081